@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <input type="button" value="Napravi obavijest" onclick="@("window.location.href='" + @Url.Action("Create", "Obavijest") + "'");" />
    </div>
